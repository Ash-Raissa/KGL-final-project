info:
  name: update produce
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:3000/procure
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
